import React from 'react'

function HomePage({ handleToggleMode }) {
  const handleSubmit = (e) => {
    e.preventDefault()

  }

  return (
    <div>
      "Hello"
    </div>
  )
}

export default HomePage
